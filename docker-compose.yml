services:
  charliework:
    build: .
    container_name: charliework
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=America/Chicago
    volumes:
      - ./data:/app/data
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: proxy
